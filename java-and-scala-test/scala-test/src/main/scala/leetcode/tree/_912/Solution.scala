package leetcode.tree._912

/**
 *
 * @author gzho
 * @since 2021-10-11 6:48 PM
 * @version 1.0.0
 */
object Solution {
  def sortArray(nums: Array[Int]): Array[Int] = {
    for(i<-nums.indices){
      for( j=i+1;;){

      }
    }
  }

  def swap(nums:Array[Int], x:Int,y:Int)={
    val temp=nums(x)
    nums(x)=nums(y)
    nums(y)=temp
  }
}
